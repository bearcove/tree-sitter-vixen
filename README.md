# tree-sitter-vixen

Tree-sitter grammar for the Vixen language, based on the specification in
`docs/content/spec/language.md`.

The grammar is intended for editor tooling first: parsing, syntax highlighting,
bracket matching, and a Zed language extension.
